data:
  input_path: data/processed/cicids2017/cic_tabular_features.csv
  target_column: target
  numeric_features: []
  categorical_features: []

split:
  test_size: 0.2
  random_seed: 42

model:
  max_iter: 1000
  class_weight: balanced

outputs:
  metrics_path: reports/logistic_cic_metrics.json
  model_path: artifacts/models/logistic_cic_baseline.joblib

experiment_tracking:
  experiment_name: aegis-hgx-logistic-cic-baseline
  uri: sqlite:///mlflow/mlflow.db
  artifact_root: mlflow/mlruns